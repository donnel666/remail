# 苹果设备码接入

系统设置 → 上游设置 → 苹果设备码配置管理 API Key、公网短信回调地址和设备平台地址。UID 固定为 919。旧资源按是否已有 `device_code_api` 选择取码方式：没有 API 时继续短信，有 API 时使用设备码；仅配置全局 API Key 或存在绑定状态不会强制迁移旧资源。新开户在配置 Key 后主动初始化设备绑定并等待成功；保存设备 API 后，后续验证不自动回退短信。命令行已完成第一阶段的旧检查点同样保留原有短信模式。

手机验证完成后保存绑定意图，最早 30 秒后导入设备平台。共享 Asynq 任务通过有时限的去重机制批量检查待绑定账号，Redis 将运行频率限制为每 10 秒一次。命令行验证器和 Web 工作流使用同一个持久化绑定队列；命令行运行时需要服务端 Asynq worker 在线。

`icloud_device_bindings` 保存尚未建成邮箱资源的命令行账号和 Web 账号的绑定状态。密码和短信回调凭证仅在绑定期间保留，绑定完成或失败即清除。邮箱资源保存 `device_code_api`、`device_bind_status`、`device_account_id`，Cookie 更新不会清空它们。列表只返回状态及可用标记，完整设备 API 经有短信查看与资源操作权限的管理接口获取。

设备绑定成功且有可用 API 后，阶段 2、阶段 3及后续 Cookie 恢复统一使用设备码。阶段间随机等待为 1～30 秒。设备码通过 Apple 的 `/auth/verify/trusteddevice/securitycode` 提交，不调用短信派发接口。

阶段 2 加入家庭后，Web 和 Go CMD 都停在 `waiting_family_sharing`，等待管理员手动开启/重置共享。CMD 再次运行默认仍停在此处，完成共享设置后用 `-family-sharing-confirmed` 明确确认，才进入阶段 3；确认结果和设备绑定信息保存在检查点中。

阶段 3 登录成功后，使用当前账号的会话查询家庭成员，核对其成员状态和账号身份。只有核验通过，才继续创建隐私邮箱及配置转发；查询失败、未加入或身份不匹配都不能继续。该核验复用现有家庭响应解析，不需要主号凭据，也不重新执行加入家庭。

手机号从第一阶段验证成功起独占 48 小时，通过 Redis `kitesim:icloud:phone-hold:<phoneID>` 的 TTL 保存，不增加数据库字段。重复确认、续跑和 Cookie 更新不会延长占用；断点恢复只写入原始成功时间对应的剩余 TTL，已过期的不重建。到期后列表与分配入口自动按可重新分配处理，保留旧账号的手机号关联。未完成验证的绑定及没有成功时间的历史绑定使用原有绑定创建时间作为有限占用的起点；缺少绑定记录的历史资源使用资源创建时间。独占不再依赖是否达到 750 个别名，因此 Cookie 异常不会造成永久占用。

Web 首次写入 Redis 失败时，保留已验证会话及对应短信记录，在 `icloud_finish` 每 10 秒重试本地写入；使用已有短信完成记录的时间，不重发短信，也不重复提交验证码。旧资源在 Cookie 更新中补建原号码关联时沿用资源的历史创建时间；新开户或实际换号仍使用分配时间，不把恢复旧关联当成一次新占用。

## 余额与充值

设置页通过 `GET /api/auth/me` 读取 `data.balance`，展示剩余 tai 点及查询时间。余额为 0 时正常显示；未配置 Key 时显示未配置，不伪装成 0。

卡密兑换调用 `POST /api/balance/recharge/card`，请求体为 `{"card_key":"卡密"}`。参数名由平台公开前端确认；充值响应按提供的 `code=0`、`data.balance` 更新余额。充值需要系统设置写入及敏感权限，使用 CSRF 和调用频率限制。卡密不入库、不写日志；充值请求不自动重试，结果不确定时先刷新余额核对。

## 待联调技术债

- TODO(device-platform): 尚无非空 `/api/accounts` 实际响应。当前 `id`、`account`、`tag`、`result_status`、`bind_uuid`、`bind_adi` 及 `cursor` 分页依据来自设备平台公开前端。仅明确的 `success` 和可用 URL 能推进；未知状态保留诊断，不当作成功。
- TODO(device-platform): 多 ID 批量查询参数未提供。目前每轮统一分页查询列表（每页 20，最多 100 页）并匹配本地账号与绑定 tag，文档明确后改为批量 ID 查询。
- TODO(device-platform): 验证导入重复账号、导入超时、失败重试时的上游行为。目前超时不自动重发导入，先查询本轮 tag，避免重复绑定；旧记录必须由上游正确处理后重试。
- TODO(device-platform): 核对设备码 API 的无验证码、离线、过期及授权错误响应。验证码提取复用短信的六位数字正则，不依赖提示文案，支持实测的 `AppleID 登录验证码:374991`；保留前导零，不从更长数字中截取六位。
- 2026-09-15 实测：5 个账号均完成阶段 2、人工开启共享、阶段 3 家庭成员核验及入库；后续登录使用设备码，家庭查询显式携带当前登录会话 Cookie。
- TODO(apple-device): 继续验证双 Cookie 恢复、设备离线及验证码过期时的恢复路径。
- TODO(device-platform): 确认设备绑定本身是否额外发送短信。设备就绪后我方不再发送短信；现有导入参数无法保证首次初始化总共只需一条短信。

设备平台账号密码、管理 Key、临时回调 Token、设备取码 URL 不写普通日志。配置的设备平台主机是唯一允许的取码目的主机，HTTP 重定向不跟随。
